# Recovery Note

## Tình trạng các tài liệu gốc
- `MH05_SmartHelmet1.docx`: Có thể đọc và trích xuất được nội dung báo cáo, bao gồm mục lục, giới thiệu, thiết kế hệ thống, thuật toán và thử nghiệm.
- `Springer-v2.docx`: Tập tin này bị lỗi định dạng ZIP / DOCX và không thể mở, giải nén hoặc trích xuất nội dung đầy đủ trong môi trường hiện tại.

## Nội dung đã sử dụng
- Báo cáo `MH05_SmartHelmet1.docx` là nguồn chính cho toàn bộ các tài liệu trong thư mục `docs/`.
- 16 hình ảnh được trích xuất vào `assets/images/` trực tiếp từ `MH05_SmartHelmet1.docx`.

## Khuyến nghị
- Nếu cần tiếp tục hoàn thiện nội dung từ `Springer-v2.docx`, hãy thử mở lại file bằng Microsoft Word trên Windows hoặc sử dụng phần mềm sửa file DOCX.
- Lưu trữ phiên bản gốc của cả hai file trong thư mục gốc để người dùng có thể tham khảo hoặc tiếp tục phục hồi sau này.
